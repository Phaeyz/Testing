name: Build Phaeyz.Testing

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build-steps.yml
    with:
      package-name: 'Phaeyz.Testing'